---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pod-gateway-config
  namespace: networking
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 100Mi
