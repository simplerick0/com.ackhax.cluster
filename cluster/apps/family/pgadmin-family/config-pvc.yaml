---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pgadmin-fam-config
  namespace: family
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
